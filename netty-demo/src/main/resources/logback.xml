<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 按天滚动 -->
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 保留30天日志 -->
            <maxHistory>30</maxHistory>
            <!-- 单个文件最大100MB -->
            <maxFileSize>100MB</maxFileSize>
        </rollingPolicy>
    </appender>

    <!-- 异步日志 -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <!-- 队列大小 -->
        <queueSize>512</queueSize>
        <!-- 队列剩余20%时丢弃TRACE、DEBUG日志 -->
        <discardingThreshold>20</discardingThreshold>
    </appender>

    <!-- 根日志级别 -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>

    <!-- 包级别日志设置 -->
    <logger name="com.example" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 特定类日志设置 -->
    <logger name="com.example.service.UserService" level="WARN"/>

    <!-- 第三方框架日志设置 -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.mybatis" level="DEBUG"/>
    <logger name="io.netty" level="debug">
    </logger>

    <logger name="com" level="INFO">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="com.shopping" level="INFO" additivity="">
        <appender-ref ref="ASYNC_FILE"/>
    </logger>

    <!--  规则总结：继承+就近原则
      级别检查：只在查找得到的effectiveLogger处进行一次
      传递机制：如果通过检查，创建的LoggingEvent会向上传递
      父Logger处理：直接使用传递来的LoggingEvent，不重新检查级别
      additivity：控制是否传递，不影响级别检查逻辑,默认为true
      -->

</configuration>